# Keep this FILENAME stable: the base-branch stats download below and the PR
# runs' artifact lookup reference nextjs-bundle-analysis.yml by name.
name: "Bundle analysis"

on:
  workflow_call:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    if: ${{ github.event_name == 'push' }}
    uses: ./.github/workflows/build.yml
    secrets: inherit
    with:
      web: true
  analyze:
    name: web (bundle size)
    needs: build
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # actions: read to download main's bundle stats from another run
    permissions:
      contents: read
      actions: read
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v7

      - name: 📥 Monorepo install
        uses: ./.github/actions/pnpm-install

      - name: 🏗️ Build
        uses: ./.github/actions/pnpm-cache-build
        with:
          cwd: ${{ github.workspace }}

      - name: 📊 Analyze bundle
        run: |
          cd apps/web
          npx -p nextjs-bundle-analysis@0.5.0 report

      - name: 📤 Upload bundle
        uses: actions/upload-artifact@v7
        with:
          name: bundle
          path: ${{ github.workspace }}/apps/web/.next/analyze/__bundle_analysis.json

      # The stats to compare with are the `bundle` artifact of this workflow's
      # latest successful run on the base branch. Runs where pr.yml calls it
      # belong to pr.yml, so only the base branch's own runs are listed.
      - name: 📥 Download base branch bundle stats
        if: success() && github.event.number
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          BASE: ${{ github.event.pull_request.base.ref }}
        run: |
          run_id=$(gh run list --workflow nextjs-bundle-analysis.yml --branch "$BASE" \
            --status success --limit 1 --json databaseId --jq '.[0].databaseId')
          gh run download "$run_id" --name bundle --dir apps/web/.next/analyze/base/bundle

      # And here's the second place - this runs after we have both the current and
      # base branch bundle stats, and will compare them to determine what changed.
      # There are two configurable arguments that come from package.json:
      #
      # - budget: optional, set a budget (bytes) against which size changes are measured
      #           it's set to 350kb here by default, as informed by the following piece:
      #           https://infrequently.org/2021/03/the-performance-inequality-gap/
      #
      # - red-status-percentage: sets the percent size increase where you get a red
      #                          status indicator, defaults to 20%
      #
      # Either of these arguments can be changed or removed by editing the `nextBundleAnalysis`
      # entry in your package.json file.
      - name: 🔄 Compare with base branch bundle
        if: success() && github.event.number
        run: |
          cd apps/web
          ls -laR .next/analyze/base && npx -p nextjs-bundle-analysis@0.5.0 compare

      # Commenting happens in nextjs-bundle-comment.yml (workflow_run), which has
      # write permissions even for fork PRs. This job only exports what it needs.
      - name: 📤 Save PR number for the comment workflow
        if: success() && github.event.number
        run: |
          echo "${{ github.event.number }}" > apps/web/.next/analyze/__pr_number

      - name: 📤 Upload comment artifact
        if: success() && github.event.number
        uses: actions/upload-artifact@v7
        with:
          name: bundle-comment
          path: |
            ${{ github.workspace }}/apps/web/.next/analyze/__bundle_analysis_comment.txt
            ${{ github.workspace }}/apps/web/.next/analyze/__pr_number
