---
name: Docker Build and Push
on:
  workflow_call:
    inputs:
      comment-enable:
        description: Create a PR comment with image tags and labels
        required: false
        default: false
        type: boolean
      context:
        description: Docker context (path) to start build from
        required: false
        type: string
      dockerhub-enable:
        description: Log into Docker Hub
        required: false
        default: false
        type: boolean
      file:
        description: Dockerfile path relative to context
        required: false
        type: string
      flavor-rules:
        description: Metadata flavor rules
        required: false
        type: string
        default: |
          latest=false
      ghcr-enable:
        description: Log into GHCR
        required: false
        default: true
        type: boolean
      image-name:
        description: Docker image name to build
        required: true
        type: string
        default: ghcr.io/${{ github.repository }}
      platforms:
        description: Platforms to build for
        required: false
        type: string
        default: linux/amd64
      push:
        description: Push image(s) to registry
        required: false
        default: true
        type: boolean
      tag-rules:
        description: Tag rules for docker/metadata-action
        required: false
        type: string
        default: |
          type=raw,value={{sha}}
          type=ref,event=tag,priority=200
          type=ref,event=pr,priority=100
      target:
        description: Docker build target stage name
        required: false
        type: string
    secrets:
      dockerhub-username:
        required: false
      dockerhub-token:
        required: false

permissions:
  contents: read
  packages: write
  pull-requests: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        platform: [linux/amd64]
    steps:
      - name: 📥 Prepare safe identifiers
        run: |
          # sanitize platform for artifact/cache scopes
          platform=${{ matrix.platform }}
          echo "PLATFORM_PAIR=${platform//\//-}" >> $GITHUB_ENV

          # sanitize image name for artifact uniqueness
          image="${{ inputs.image-name }}"
          safe_image=${image//\//-}
          echo "SAFE_IMAGE=$safe_image" >> $GITHUB_ENV

      - uses: actions/checkout@v4
        with:
          submodules: false

      - name: 🐹 Set up QEMU for cross-arch builds
        uses: docker/setup-qemu-action@v3
        with:
          platforms: all

      - name: 🐳 Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v3

      - name: 🐳 Login to DockerHub
        if: inputs.dockerhub-enable
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.dockerhub-username }}
          password: ${{ secrets.dockerhub-token }}

      - name: 📦 Login to GHCR
        if: inputs.ghcr-enable
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐳 Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ inputs.image-name }}
          flavor: ${{ inputs.flavor-rules }}
          tags: ${{ inputs.tag-rules }}

      - name: 🐳 Build and Push per-platform (digest only)
        id: build
        uses: docker/build-push-action@v6
        with:
          provenance: false
          platforms: ${{ matrix.platform }}
          # Default to the checked-out workspace. With no context,
          # build-push-action falls back to the Git context and BuildKit
          # re-clones the whole repo (~1.5 GB) inside the builder, discarding
          # the checkout above.
          context: ${{ inputs.context || '.' }}
          file: ${{ inputs.file }}
          target: ${{ inputs.target }}
          push: true
          outputs: type=image,name=${{ inputs.image-name }},push-by-digest=true,name-canonical=true,push=true
          labels: ${{ steps.meta.outputs.labels }}
          annotations: ${{ steps.meta.outputs.annotations }}
          cache-from: type=registry,ref=${{ inputs.image-name }}:buildcache
          cache-to: type=registry,ref=${{ inputs.image-name }}:buildcache,mode=max

      - name: 📤 Export digest
        run: |
          mkdir -p /tmp/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "/tmp/digests/${digest#sha256:}"

      - name: 📤 Upload digest
        uses: actions/upload-artifact@v4
        with:
          name: digests-${{ env.SAFE_IMAGE }}-${{ env.PLATFORM_PAIR }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

      # --- Optional PR comment
      - name: 💬 Find PR comment
        if: github.event_name == 'pull_request' && inputs.comment-enable
        id: fc
        uses: peter-evans/find-comment@v4
        with:
          issue-number: ${{ github.event.pull_request.number }}
          comment-author: github-actions[bot]
          body-includes: "📦 Docker image tags pushed"

      - name: 💬 Create/update PR comment
        if: github.event_name == 'pull_request' && inputs.comment-enable
        uses: peter-evans/create-or-update-comment@v5
        with:
          comment-id: ${{ steps.fc.outputs.comment-id }}
          issue-number: ${{ github.event.pull_request.number }}
          edit-mode: replace
          body: |
            📦 Docker image tags pushed:
            ```text
            ${{ steps.meta.outputs.tags }}
            ```
            🏷 Labels added:
            ```text
            ${{ steps.meta.outputs.labels }}
            ```

      - name: 🏷 Output primary image tag
        id: image-tag
        run: echo "image-tag=${{ steps.meta.outputs.version }}" >> $GITHUB_OUTPUT

  merge:
    runs-on: ubuntu-latest
    needs: build
    if: inputs.push
    steps:
      - name: 📥 Prepare safe image identifier
        run: |
          image="${{ inputs.image-name }}"
          safe_image=${image//\//-}
          echo "SAFE_IMAGE=$safe_image" >> $GITHUB_ENV

      - name: 📥 Download digests
        uses: actions/download-artifact@v4
        with:
          path: /tmp/digests
          # ✅ only grab digests for THIS image
          pattern: digests-${{ env.SAFE_IMAGE }}-*
          merge-multiple: true

      - name: 🐳 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🐳 Login to DockerHub
        if: inputs.dockerhub-enable
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.dockerhub-username }}
          password: ${{ secrets.dockerhub-token }}

      - name: 📦 Login to GHCR
        if: inputs.ghcr-enable
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/checkout@v4
        with:
          submodules: false
          fetch-depth: 0
          fetch-tags: true

      # `latest` means the newest stable release. A tag earns it only when it is
      # the highest vX.Y.Z in the repository, so a patch release on a maintenance
      # branch never overtakes a newer major and pre-release tags never qualify.
      - name: 🏷️ Decide whether this tag becomes latest
        id: latest
        run: |
          latest=false
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            highest=$(git tag --list 'v*' | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' | sort -V | tail -n 1)
            [[ "$GITHUB_REF_NAME" == "$highest" ]] && latest=true
          fi
          echo "latest=$latest" >> "$GITHUB_OUTPUT"
          echo "tag=$GITHUB_REF_NAME highest=${highest:-none} latest=$latest"

      - name: 🐳 Docker metadata extraction
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ inputs.image-name }}
          flavor: ${{ inputs.flavor-rules }}
          tags: |
            ${{ inputs.tag-rules }}
            type=raw,value=latest,enable=${{ steps.latest.outputs.latest == 'true' }}

      - name: 🐳 Create and push multi-arch manifest
        working-directory: /tmp/digests
        run: |
          echo "Merging digests for ${{ inputs.image-name }}..."
          ls -l .
          docker buildx imagetools create \
            $(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON") \
            $(printf '${{ inputs.image-name }}@sha256:%s ' *)

      - name: 🐳 Inspect Built Image
        run: docker buildx imagetools inspect ${{ inputs.image-name }}:${{ steps.meta.outputs.version }}
